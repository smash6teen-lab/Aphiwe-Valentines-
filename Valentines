<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Be My Valentine ❤️</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#fff5f7;
      --card:#ffffff;
      --accent:#ff4d6d;
      --accent-soft:#ffd6de;
      --text:#2b2b2b;
    }
    *{box-sizing:border-box}
    body{
      margin:0;min-height:100vh;display:grid;place-items:center;
      font-family:Inter, system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      background:radial-gradient(1200px 600px at 10% 10%, #ffe3ea 0%, transparent 60%),
                 radial-gradient(1000px 500px at 90% 90%, #ffe9ef 0%, transparent 55%),
                 var(--bg);
      color:var(--text);
    }
    .card{
      background:var(--card);
      border-radius:24px;
      box-shadow:0 20px 40px rgba(0,0,0,.08);
      max-width:680px;width:92%;padding:28px 28px 36px;
      position:relative;overflow:hidden;
    }
    .badge{
      position:absolute;top:18px;right:18px;
      background:var(--accent-soft);color:var(--accent);
      padding:6px 12px;border-radius:999px;font-weight:600;font-size:12px;
    }

